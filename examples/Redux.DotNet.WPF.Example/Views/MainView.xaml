<Window x:Class="ReduxSharp.WPF.Example.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Name="Window"
        Title="MainView" Height="450" Width="800">
    <StackPanel>

        <TextBlock Text="HE"/>
        <StoreProvider Store="{Binding Store, ElementName=Window}">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Value: " FontSize="40"/>
                    <TextBlock Text="{StoreBinding IntValue}"  FontSize="40"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal"  Height="40">
                    <Button Content="Increment" Width="100" Margin="2" Command="{Binding IncrementCommand}"/>
                    <Button Content="Decrement" Width="100" Margin="2" Command="{Binding DecrementCommand}"/>
                </StackPanel>
            </StackPanel>
        </StoreProvider>
    </StackPanel>
</Window>
